import { SortCompetitorsIndividualsFirstPipe } from './sort-competitors-individuals-first.pipe';

describe('SortCompetitorsIndividualsFirstPipe', () => {
  it('create an instance', () => {
    const pipe = new SortCompetitorsIndividualsFirstPipe();
    expect(pipe).toBeTruthy();
  });
});
