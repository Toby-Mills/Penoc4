import { SortResultsByDatePipe } from './sort-results-by-date.pipe';

describe('SortResultsByDatePipe', () => {
  it('create an instance', () => {
    const pipe = new SortResultsByDatePipe();
    expect(pipe).toBeTruthy();
  });
});
