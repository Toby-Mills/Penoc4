import { SortEventDatePipe } from './sort-event-date.pipe';

describe('SortEventDatePipe', () => {
  it('create an instance', () => {
    const pipe = new SortEventDatePipe();
    expect(pipe).toBeTruthy();
  });
});
