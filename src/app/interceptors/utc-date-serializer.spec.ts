import { UTCDateSerializer } from './utc-date-serializer';

describe('UTCDateSerializer', () => {
  it('should create an instance', () => {
    expect(new UTCDateSerializer()).toBeTruthy();
  });
});
